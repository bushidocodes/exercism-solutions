#pragma once

#include <cstdint>

namespace difference_of_squares
{
    uint64_t square_of_sum(uint32_t);
    uint64_t sum_of_squares(uint32_t);
    uint64_t difference(uint32_t);

} // namespace difference_of_squares
