#pragma once

namespace triangle
{
    enum flavor
    {
        equilateral,
        isosceles,
        scalene
    };

    enum flavor
    kind(double a_length, double b_length, double c_length);

} // namespace triangle
